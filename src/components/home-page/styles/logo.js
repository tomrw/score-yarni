import { StyleSheet } from 'react-native';

const height = 100;

export default StyleSheet.create({
	container: {
		backgroundColor: '#ccc',
		height,
		width: '100%',
		marginBottom: 10,
		justifyContent: 'center'
	},
	text: {
		textAlign: 'center'
	}
});
