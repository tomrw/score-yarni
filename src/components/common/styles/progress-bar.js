import { StyleSheet } from 'react-native';

const height = 5;

export default StyleSheet.create({
	container: {
		backgroundColor: '#ccc',
		height,
		width: 100
	},
	bar: {
		backgroundColor: 'red',
		height,
		width: 100
	}
});
